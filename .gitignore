# Diretório de estado do Terraform
.terraform/
.terraform.lock.hcl

# Arquivos de estado do Terraform (contém informações sensíveis)
terraform.tfstate
terraform.tfstate.backup
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.backup

# Arquivos de variáveis sensíveis (caso contenha credenciais)
*.tfvars
!example.tfvars # Mantém um exemplo para referência

# Logs e arquivos temporários
crash.log
*.log
*.backup
*.bak

# Chaves SSH e credenciais sensíveis
*.pem
*.key
*.crt

# Configurações locais do Terraform
terraform.rc
terraform.tfvars.json

# Arquivos de saída do Terraform
plan.out

# Diretório de cache do Terraform (caso utilize módulos locais)
**/.terraform/
